package net.uchuika.v1_19_3;

import com.mojang.blaze3d.vertex.PoseStack;
import net.labymod.api.models.Implements;
import net.uchuika.core.listener.AnniActionbarListener;
import net.uchuika.core.listener.RenderRankTag;
import org.jetbrains.annotations.NotNull;
import org.joml.Matrix4f;
import javax.inject.Singleton;

/*
@Singleton
@Implements(VersionRenderRankTag.class)

public class VersionRenderRankTag implements RenderRankTag {


  @Override
  public @NotNull void renderRankTag() {
    PoseStack poseStack = new PoseStack();

    poseStack.pushPose();
    poseStack.translate(0.0F, 0.5F, 0.0F);
    poseStack.mulPose(this.entityRenderDispatcher.cameraOrientation());
    poseStack.scale(-0.025F, -0.025F, 0.025F);
    Matrix4f matrix4f = p_114500_.last().pose();
  }


}

 */

